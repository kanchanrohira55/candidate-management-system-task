{
  "name": "fresh-shifts-candidate-management",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "node backend/node_modules/prisma/build/index.js generate --schema backend/prisma/schema.prisma && npm --workspace frontend run build",
    "test": "npm --workspace backend test"
  }
}
